package data.structure

class RangeModule {
    val range = ArrayList<IntArray>()

    fun addRange(left: Int, right: Int) {

    }

    fun queryRange(left: Int, right: Int): Boolean {
        return true
    }

    fun removeRange(left: Int, right: Int) {

    }
}